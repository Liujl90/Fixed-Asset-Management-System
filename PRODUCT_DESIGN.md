# 基于 SpringBoot 的企业固定资产全生命周期管理系统

## MVP 产品设计文档

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.0 |
| 产品阶段 | MVP |
| 系统定位 | 面向中小型企业的固定资产全生命周期管理系统 |
| 主要技术方向 | Spring Boot、MyBatis-Plus、MySQL、Vue 3、Element Plus、Spring Security、JWT、ECharts |
| 文档目的 | 明确 MVP 的产品范围、用户角色、核心流程、业务对象和验收标准 |

---

## 1. 项目概述

### 1.1 产品定位

本系统用于帮助中小型企业完成固定资产从登记、领用、归还、调拨到统计分析的数字化管理。MVP 阶段不追求覆盖企业资产管理的全部环节，而是优先建立一个可运行、可演示、可继续扩展的业务闭环。

系统需要回答以下核心问题：

- 企业当前有哪些固定资产？
- 资产由哪个部门、哪名员工负责？
- 资产当前处于闲置、在用、维修中还是报废状态？
- 资产发生了哪些领用、归还和调拨行为？
- 企业资产在部门、状态和数量上的整体分布如何？

### 1.2 主要用户

- 需要统一维护企业资产信息的管理员。
- 需要查看和处理资产领用、归还和调拨的管理员。
- 需要申请领用、归还资产并查看个人相关记录的普通员工。

### 1.3 MVP 产品原则

1. **业务闭环优先**：优先保证组织、资产、领用、归还、调拨和统计形成完整链路。
2. **边界清晰**：暂不建设采购、维修、盘点、折旧等复杂业务流程。
3. **数据一致**：资产状态、使用部门和负责人必须与业务记录保持一致。
4. **实现可控**：采用单体应用和常规分层设计，避免不必要的微服务、消息队列等复杂架构。
5. **便于扩展**：业务对象和状态模型应为后续完整生命周期管理预留空间。

### 1.4 系统边界

MVP 是一个企业内部管理系统，不是财务系统，也不是采购、仓储或工单系统。其核心价值是管理“已经登记在系统中的固定资产”在组织内部的归属和使用状态。

---

## 2. 项目目标

### 2.1 MVP 总目标

完成一个可以实际运行的最小可用版本，实现以下核心业务链路：

> 用户登录 → 部门/员工 → 资产分类 → 资产登记 → 资产查询 → 资产领用 → 资产归还 → 资产调拨 → Dashboard 数据统计

### 2.2 具体目标

- 建立基础的用户、角色和菜单权限体系。
- 建立部门和员工基础数据，为资产归属和领用提供组织基础。
- 支持资产分类的分层维护。
- 支持固定资产基本信息、状态、所属部门和负责人的维护。
- 支持资产领用、归还和调拨，并自动维护资产状态与归属信息。
- 支持按常用条件分页查询资产和业务记录。
- 通过 Dashboard 展示资产总量、状态分布、部门分布和最近变动。
- 形成后续扩展采购、维修、盘点、折旧等能力的基础框架。

### 2.3 成功判断标准

MVP 完成后，演示人员应能够从管理员或普通员工的视角完整走通一条资产业务链路，并在 Dashboard 中看到与业务操作一致的数据结果。

---

## 3. 用户角色

MVP 预设“管理员”和“普通员工”两类基础业务角色。管理员内部可以继续通过角色和菜单权限进行配置，但 MVP 不建设复杂的数据权限体系。

### 3.1 角色定义

| 角色 | 主要职责 | 典型权限 |
| --- | --- | --- |
| 管理员 | 系统基础数据、资产全流程和统计管理 | 用户、角色、菜单、部门、员工、分类、资产、领用、归还、调拨、Dashboard |
| 普通员工 | 参与个人相关资产业务 | 登录、退出、个人信息、提交领用/归还申请、查看个人资产和业务记录 |

### 3.2 权限原则

- 未登录用户只能访问登录页面。
- 普通员工只能访问个人相关页面和个人业务记录。
- 管理员拥有 MVP 范围内的业务管理权限。
- 菜单权限和操作权限用于控制页面及关键操作，不扩展到按部门、按数据行控制的复杂数据权限。
- 角色管理用于配置角色与菜单/操作权限的对应关系。

### 3.3 用户与员工的关系

“用户”是登录系统的账号，“员工”是参与组织与资产业务的人员档案。两者在业务上可以建立一一关联：

- 普通员工账号应关联一名员工。
- 管理员的账号可以不关联业务员工，也可以关联，取决于企业实际使用方式。
- 资产负责人、领用人和调拨对象均指向员工档案，而不是直接指向登录账号。

---

## 4. MVP 功能架构

```text
企业固定资产全生命周期管理系统（MVP）
├── 用户与权限管理
│   ├── 登录/退出
│   ├── 用户管理
│   ├── 角色管理
│   ├── 菜单/操作权限
│   └── 个人信息
├── 部门与员工管理
│   ├── 部门管理
│   ├── 员工管理
│   └── 员工状态与所属部门
├── 资产基础资料
│   └── 资产分类管理
├── 固定资产管理
│   ├── 资产新增、编辑、删除、详情
│   ├── 资产分页和多条件查询
│   └── 资产基础信息维护
├── 资产业务管理
│   ├── 领用申请与处理
│   ├── 归还申请与确认
│   └── 部门/人员调拨
└── Dashboard
    ├── 资产总量与状态概览
    ├── 部门资产分布
    ├── 资产状态占比
    └── 最近资产变动记录
```

MVP 模块之间以“部门、员工、分类、资产”为基础数据，以“领用记录、归还记录、调拨记录、资产变动记录”为业务数据，最终汇总到 Dashboard。

---

## 5. 核心业务流程

### 5.1 登录与授权流程

1. 用户输入账号和密码。
2. 系统完成身份认证并返回登录态。
3. 系统加载用户角色和菜单/操作权限。
4. 用户进入有权限访问的页面。
5. 用户退出后登录态失效。

MVP 需要保证未登录访问和越权访问被拒绝。

### 5.2 基础数据准备流程

1. 管理员维护部门。
2. 管理员新增员工，并设置员工所属部门和状态。
3. 管理员维护资产分类，支持层级结构。
4. 管理员登记固定资产，填写基础信息并设置初始状态。

部门、员工和分类是资产登记、领用、归还和调拨的前置数据。

### 5.3 资产登记流程

1. 管理员新增资产。
2. 填写资产编号、名称、分类、品牌型号、购买日期、原值、使用年限等信息。
3. 设置所属部门、当前负责人和资产状态。
4. 系统校验资产编号唯一性。
5. 保存后，资产进入可查询、可统计的正常管理状态。

新增资产默认建议为“闲置”。如果初始为“在用”，应同时填写所属部门和负责人。

### 5.4 资产领用与归还流程

MVP 使用单级管理员确认，不建设通用审批流引擎。

领用流程：

1. 普通员工选择闲置资产并提交领用申请，或由管理员代员工登记领用。
2. 管理员查看待处理申请。
3. 管理员确认领用后，系统记录申请人、资产、领用时间、使用部门和负责人。
4. 资产状态由“闲置”变为“在用”。
5. 系统生成资产变动记录。

归还流程：

1. 员工发起归还，或由管理员直接登记归还。
2. 管理员确认资产已收回。
3. 系统记录归还时间，并结束对应领用关系。
4. 资产状态由“在用”变为“闲置”。
5. 资产当前负责人和当前使用关系按业务规则更新。
6. 系统生成资产变动记录。

关键规则：

- 同一资产不能同时存在多条有效的在用领用记录。
- 只有“闲置”资产可以被领用。
- 只有存在有效领用关系的资产可以归还。
- 资产状态变化和领用/归还记录应在同一次业务操作中完成。

### 5.5 资产调拨流程

1. 管理员选择在用资产发起调拨。
2. 填写原部门、目标部门、原负责人、新负责人和调拨原因。
3. 管理员确认调拨。
4. 系统保存调拨记录和调拨时间。
5. 系统将资产所属部门更新为目标部门，将当前负责人更新为新负责人。
6. 资产状态保持“在用”。
7. 系统生成资产变动记录。

关键规则：

- 调拨必须发生在不同部门或不同负责人之间。
- 调拨完成后，资产当前归属必须与最新调拨记录一致。
- 调拨记录必须可追溯。
- MVP 不建设多级审批和跨组织审批。

### 5.6 Dashboard 更新流程

Dashboard 不单独维护统计数据，而是从当前资产数据和业务记录中实时或准实时汇总：

- 资产总量和状态数量来自当前资产数据。
- 部门资产分布来自资产当前所属部门。
- 状态占比来自资产当前状态。
- 最近资产变动来自资产变动记录。

---

## 6. 各模块功能概述

### 6.1 用户与权限管理

**目标**：提供系统登录、账号管理和基础权限控制。

MVP 功能：

- 用户登录、退出和登录状态校验。
- 用户新增、编辑、查询和状态管理。
- 角色新增、编辑、查询和权限配置。
- 菜单权限和基础操作权限控制。
- 管理员与普通员工的基础权限区分。
- 查看和修改个人信息。

关键规则：

- 已停用用户不能登录。
- 不允许删除仍被业务数据引用的用户。
- 密码必须满足基础安全要求。
- 普通员工不能访问管理端配置页面。

### 6.2 部门与员工管理

**目标**：建立企业组织结构和资产责任人基础数据。

MVP 功能：

- 部门新增、编辑、删除、查询。
- 员工新增、编辑、查询和状态管理。
- 设置员工所属部门。
- 维护员工在职、离职等基础状态。

关键规则：

- 存在下级部门、员工或资产引用时，部门不能直接删除。
- 离职员工不能被设置为新的资产负责人。
- 员工所属部门用于资产默认归属和业务筛选。

### 6.3 资产分类管理

**目标**：对固定资产进行分层归类，如办公设备、生产设备、办公家具等。

MVP 功能：

- 分类新增、编辑、删除和查询。
- 支持父级分类和子级分类。
- 资产登记时选择分类。
- 按分类统计和查询资产。

关键规则：

- 分类名称在同一层级下应保持唯一。
- 存在子分类或已被资产使用的情况，不允许直接删除。
- 分类层级应控制合理深度，避免无限复杂化。

### 6.4 固定资产管理

**目标**：维护企业固定资产的核心档案，是其他业务模块的数据中心。

MVP 功能：

- 资产新增、编辑、删除和详情查看。
- 资产分页查询。
- 按资产编号、名称、分类、状态、部门等条件查询。
- 维护资产基础信息。
- 查看资产当前归属、当前负责人和最近业务变动。

资产基础信息至少包括：

| 信息项 | 说明 |
| --- | --- |
| 资产编号 | 企业内唯一标识 |
| 资产名称 | 资产的业务名称 |
| 资产分类 | 资产所属分类 |
| 品牌/型号 | 用于辅助识别资产 |
| 购买日期 | 资产购置时间 |
| 原值 | 资产购置金额 |
| 使用年限 | 用于展示和后续扩展折旧能力 |
| 所属部门 | 资产当前归属部门 |
| 当前负责人 | 资产当前使用或管理责任人 |
| 资产状态 | 闲置、在用、维修中、报废 |
| 备注 | 补充说明 |

关键规则：

- 资产编号必须唯一。
- 资产状态为“在用”时，应具备所属部门和负责人。
- 已产生业务记录的资产不应被无痕物理删除。
- MVP 仅记录原值和使用年限，不实现完整财务折旧计算。

### 6.5 资产领用与归还

**目标**：记录资产在员工之间的使用关系，并保证资产状态一致性。

MVP 功能：

- 员工提交领用申请。
- 管理员处理领用申请。
- 管理员代员工登记领用。
- 员工发起归还。
- 管理员确认归还。
- 查询领用和归还记录。
- 查看当前在用资产和个人相关记录。

MVP 记录至少包括：

- 申请人。
- 资产。
- 领用时间。
- 归还时间。
- 使用部门。
- 负责人。
- 业务状态。
- 备注。

关键规则：

- 领用成功时资产变为“在用”。
- 归还成功后资产变为“闲置”。
- 不允许对非闲置资产重复领用。
- 不允许归还不存在有效领用关系的资产。

### 6.6 资产调拨

**目标**：记录资产在企业内部不同部门、不同负责人之间的归属变化。

MVP 功能：

- 发起资产调拨。
- 记录原部门和目标部门。
- 记录原负责人和新负责人。
- 记录调拨原因和调拨时间。
- 查询调拨记录。
- 调拨完成后更新资产当前所属部门和负责人。

MVP 调拨记录至少包括：

- 资产。
- 原部门。
- 目标部门。
- 原负责人。
- 新负责人。
- 调拨原因。
- 调拨时间。

关键规则：

- 只有具备明确当前归属的资产可以发起调拨。
- 调拨完成后资产状态仍为“在用”。
- 调拨操作必须同步更新资产归属，不能只保存记录。

### 6.7 Dashboard 数据统计

**目标**：为管理员提供资产整体情况和近期变动的快速概览。

MVP 功能：

- 资产总数。
- 在用资产数量。
- 闲置资产数量。
- 维修中资产数量。
- 报废资产数量。
- 各部门资产数量图表。
- 不同状态资产占比图表。
- 最近资产变动记录。

展示原则：

- 使用 ECharts 完成基础可视化。
- 指标必须来自真实业务数据。
- 数据定义保持一致，避免同一指标在不同页面出现不同口径。
- MVP 不建设复杂 BI、自定义报表和多维钻取能力。

---

## 7. 核心业务对象

本节只描述业务对象及关系，不设计数据库表结构和接口。

| 业务对象 | 业务含义 | 关键关系 |
| --- | --- | --- |
| 用户 | 系统登录账号 | 关联角色，可选关联员工 |
| 角色 | 一组菜单和操作权限 | 关联用户和权限 |
| 部门 | 企业组织结构 | 关联员工和资产 |
| 员工 | 参与资产使用和负责的人员档案 | 所属部门，可关联用户 |
| 资产分类 | 资产归类的层级目录 | 关联资产，可拥有父子分类 |
| 固定资产 | 企业资产核心档案 | 关联分类、部门、负责人；产生领用、归还、调拨和变动记录 |
| 领用/归还记录 | 描述资产被员工使用和归还的过程 | 关联资产、员工、部门 |
| 调拨记录 | 描述资产归属变更的过程 | 关联资产、原部门、目标部门、原负责人、新负责人 |
| 资产变动记录 | 汇总关键资产变化，供追溯和 Dashboard 使用 | 关联资产及对应业务记录 |

核心关系说明：

- 一个部门可以包含多个员工。
- 一个员工通常属于一个部门。
- 一个资产分类可以包含多个子分类和多个资产。
- 一个资产在同一时刻只应有一个当前状态、一个当前所属部门和一个当前负责人。
- 一个资产可以拥有多条历史领用、归还和调拨记录。
- 一个用户可以通过角色获得多项权限。

---

## 8. 主要资产状态及状态变化

### 8.1 MVP 资产状态

| 状态 | 含义 |
| --- | --- |
| 闲置 | 资产可用，尚未被员工领用 |
| 在用 | 资产已由员工或部门使用 |
| 维修中 | 资产暂时不可正常使用，处于维修状态 |
| 报废 | 资产已不再作为正常可用资产管理 |

### 8.2 状态变化规则

| 当前状态 | 目标状态 | 触发方式 |
| --- | --- | --- |
| 闲置 | 在用 | 领用确认 |
| 在用 | 闲置 | 归还确认 |
| 闲置/在用 | 维修中 | 管理员在资产信息中维护状态 |
| 闲置/在用/维修中 | 报废 | 管理员在资产信息中维护状态 |
| 维修中 | 闲置/在用 | 管理员在资产信息中修正状态 |

说明：

- “领用”和“归还”是 MVP 的核心自动状态变化。
- MVP 不建设维修工单和报废审批流程，“维修中”和“报废”主要通过资产信息维护表达。
- 任何关键状态变化都应产生资产变动记录。
- 报废状态在 MVP 中视为终止状态，不允许再发起领用和调拨。

### 8.3 状态一致性要求

- 资产状态与有效领用关系不能冲突。
- 资产为“在用”时，应能确定当前使用部门和负责人。
- 资产为“闲置”时，不应存在有效领用关系。
- 调拨不会改变“在用”状态，但会改变部门和负责人。
- 维修中或报废资产不能被普通员工领用。

---

## 9. MVP 范围与非 MVP 范围

### 9.1 MVP 范围

- 用户登录、退出、个人信息。
- 用户、角色、菜单和基础权限控制。
- 管理员与普通员工的基础权限区分。
- 部门管理和员工管理。
- 资产分类及其层级管理。
- 固定资产新增、编辑、删除、详情和分页查询。
- 按编号、名称、分类、状态、部门等条件查询资产。
- 资产领用、归还及其记录查询。
- 资产调拨及其记录查询。
- 资产状态和当前归属的一致性维护。
- 基础资产变动记录。
- Dashboard 核心指标、图表和最近变动展示。

### 9.2 暂不纳入 MVP 的功能

- 采购管理。
- 入库管理。
- 维修管理。
- 保养管理。
- 资产盘点。
- 资产报废流程。
- 完整折旧计算。
- 资产利用优化算法。
- Excel 批量导入导出。
- 二维码。
- 文件或图片管理。
- MinIO。
- 复杂消息通知。
- 复杂数据权限。
- 多级通用审批流。
- 微服务、消息队列、分布式事务等复杂技术架构。

### 9.3 范围控制原则

- 新增功能必须直接服务于 MVP 核心业务闭环。
- “维修中”和“报废”只作为资产状态和统计口径存在，不扩展为完整业务模块。
- 允许为后续能力预留字段和业务概念，但不在 MVP 中实现对应流程。
- 不因技术演示而增加与固定资产管理无关的模块。

---

## 10. 后续 V2/V3 扩展方向

### 10.1 V2：完善资产生命周期

- 采购与入库登记。
- 维修和保养记录。
- 资产盘点任务及差异处理。
- 资产报废申请和审批。
- 基础折旧计算与资产净值展示。
- Excel 批量导入导出。
- 资产图片和附件管理。
- 二维码生成与扫码查询。
- 关键业务节点消息提醒。
- 更细粒度的角色和数据权限。

### 10.2 V3：增强分析与智能化

- 资产全生命周期时间线。
- 到期、维保、闲置等预警。
- 资产利用率和闲置分析。
- 资产配置优化建议。
- 更丰富的统计报表和可配置看板。
- 移动端或企业应用集成。
- 与其他企业管理系统集成。

### 10.3 演进原则

V2 和 V3 应基于 MVP 已有的部门、员工、分类、资产、业务记录和状态模型逐步扩展，避免在 MVP 阶段提前引入超出当前业务需要的复杂流程和技术组件。

---

## 11. MVP 验收标准

### 11.1 功能完整性

- 管理员可以正常登录、退出并访问被授权的功能。
- 普通员工只能访问个人相关功能和记录。
- 可以维护部门、员工和资产分类。
- 可以新增、编辑、查询、查看和删除符合规则的资产。
- 资产查询支持分页，并支持编号、名称、分类、状态、部门等条件。
- 可以完成一次资产领用，领用后资产状态变为“在用”。
- 可以完成一次资产归还，归还后资产状态变为“闲置”。
- 可以完成一次资产调拨，调拨后部门和负责人正确变化。
- 可以查询领用、归还和调拨记录。
- Dashboard 能正确展示资产总数、各状态数量、部门分布、状态占比和最近变动。

### 11.2 业务一致性

- 资产编号在系统中保持唯一。
- 同一资产不能同时存在多条有效领用关系。
- 资产状态变化与领用、归还操作保持一致。
- 调拨记录与资产当前部门和负责人保持一致。
- 删除部门、员工、分类或资产时，不破坏已有业务数据。
- Dashboard 统计结果与资产列表和业务记录一致。

### 11.3 权限与安全性

- 未登录用户不能访问受保护功能。
- 普通员工不能执行管理员操作。
- 密码、登录态和接口访问具备基础安全控制。
- 关键业务操作具备服务端权限校验，不能仅依赖前端隐藏按钮。

### 11.4 可用性与可演示性

- 核心页面具备清晰的列表、表单、查询和反馈状态。
- 主要业务操作具备成功、失败和校验提示。
- 管理员可以按照核心链路完成完整演示。
- 系统在常规演示数据量下能够正常响应。

### 11.5 工程质量与验证

- 核心业务规则具备自动化测试或明确的手工验收用例。
- 领用、归还、调拨和状态一致性属于重点验证范围。
- 交付前相关测试和验证全部通过。
- 项目可在本地完成启动、初始化和核心功能演示。

### 11.6 MVP 完成定义

当系统能够稳定完成以下完整链路，并且链路中的数据保持一致时，可认为 MVP 达成：

> 管理员登录并准备部门、员工和分类 → 登记资产 → 员工申请领用 → 管理员确认领用 → 资产变为在用 → 员工归还或管理员调拨 → 资产归属和状态正确更新 → Dashboard 展示正确统计与最近变动

---

## 附录：MVP 关键决策摘要

- MVP 采用单体系统，不做微服务拆分。
- 角色以管理员和普通员工为主，权限控制在菜单和操作层面。
- 用户与员工分离，业务责任人指向员工。
- 维修和报废在 MVP 中只作为资产状态存在，不实现对应业务流程。
- 领用和归还使用单级管理员确认，不建设通用审批引擎。
- 调拨采用直接确认并更新资产归属的方式。
- Dashboard 只做基础统计可视化，不建设复杂 BI。
- MVP 完成后，再按 V2/V3 方向逐步扩展完整生命周期能力。
