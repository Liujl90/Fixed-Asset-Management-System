spring:
  datasource:
    url: jdbc:h2:mem:fixedasset;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: true

app:
  jwt:
    secret: fixed-asset-integration-test-secret-key-2026
    expiration-minutes: 30
